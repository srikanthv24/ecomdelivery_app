import React from "react";
import { FormControl } from "react-bootstrap";
import DatePicker from "react-multi-date-picker";

export const DateInput = () => {
  return <FormControl type="date" style={{ padding: "3px" }} />;
};
